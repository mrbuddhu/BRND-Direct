# Supabase (Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkXVCJ9...

# Never expose the service role key in the browser — use only in Server Actions
# or Route Handlers if you need to bypass RLS for admin jobs:
# SUPABASE_SERVICE_ROLE_KEY=
